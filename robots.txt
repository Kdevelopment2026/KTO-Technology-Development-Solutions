User-agent: *
Allow: /

# thanks.html and 404.html carry `noindex` in their own <head>, which is the
# directive that actually keeps them out of results. A Disallow line here used
# to sit alongside it and worked against it: a disallowed URL is never fetched,
# so the noindex is never read, and the URL can still be listed bare. Let the
# meta tag do the job.

Sitemap: https://kto-technology.netlify.app/sitemap.xml
